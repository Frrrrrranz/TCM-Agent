# 贡献指南

感谢参与 TCM-Agent。改动应同时考虑软件正确性、医疗安全边界和数据可追溯性。

## 开发原则

- 先阅读相关源码、配置和测试，再修改行为。
- 医疗事实优先由结构化数据或检索工具提供，提示词不得伪造证据。
- 新增公开输入必须校验；错误响应不得泄露密钥、路径或堆栈。
- 不提交调试日志、缓存、临时数据库、测试产物或本地凭据。
- 修改用户可见行为时补充测试和文档。

## 本地验证

```powershell
npm run check
npm test
cd web
npm run build
cd ..\tcm-mcp-server
$env:PYTEST_DISABLE_PLUGIN_AUTOLOAD='1'
pytest tests -q
```

提交前还应运行 `npm run lint`。若现有基线存在失败，请在 PR 中明确区分基线问题与新增问题。

## 代码约定

- TypeScript 避免 `any`，优先明确类型与 early return。
- Python 使用完整类型标注、Pydantic 校验和 `logging`。
- 异步独立操作应并行；外部 API、数据库和模型调用必须有失败处理。
- 注释解释设计原因、风险和边界，不复述代码。

## 医疗领域改动

调整辨证、方剂、中药、配伍或剂量规则时，请提供：

1. 规则来源和适用范围；
2. 正例、反例及边界用例；
3. 高风险人群与禁忌处理；
4. 失败或证据不足时的降级文案。

任何输出都必须保持“辅助分析而非诊疗结论”的产品边界。

## 提交说明

PR 描述应包含问题、方案、关键取舍、验证命令和残余风险。尽量保持单一主题，避免将无关格式化或生成文件混入功能提交。
